{{-- <div class="form-check form-check-inline"> --}}
<div class="form-control-inline">
    <input class="" type="radio" name="{{ $name }}" id="{{ $name }}" value="{{ $value }}">
    <label class="" for="{{ $name }}">{{ $value }}</label>
</div>
